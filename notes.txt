npm install vite@6 = install vite
npm create vite@6 = create project in vite
npm i = intall npm
npm run dev

*push to git*
echo "# fst_day1" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin (add your url gir here)
git push -u origin main

*python & django*
pip -V = check python version   
py -m venv venv = install virtual environment (!important)
venv/scripts/activate = activate the venv (!important)
pip install django = install django
django-admin = show commands
django-admin startproject backend = create project
django-admin startapp base = create app(function)
py manage.py runserver = run app
pip install djangorestframework =  download and install DRF into your environment.
pip install pillow
py manage.py migrate = commit plan
py manage.py makemigrations = save
deactivate = stop virtual environment
pip install django-cors-headers = install cors headers
railway.com

*day 3*
pip install djangorestframework-simplejwt = install package
pip install rest_framework_simplejwt = install simplejwt
rest_framework_simplejwt

*If pip is missing for some reason, you can install it with*
curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py
python get-pip.py

*day 4*
3
8003